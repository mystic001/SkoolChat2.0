<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mystic.skoolchat20">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".RegisterAsOwnerActivity" />
        <activity android:name=".OwnerActivity" />
        <activity android:name=".WelcomeAdminActivity" />
        <activity android:name=".RegisterAsAdminActivity" />
        <activity android:name=".SelectUserTypeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConfirmAdminActivity" />
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".RegisterSchoolActivity" />
        <activity android:name=".UserDashboardActivity" />
        <activity android:name=".SkoolActivity" />
        <activity android:name=".RegistrationActivity" />
    </application>

</manifest>